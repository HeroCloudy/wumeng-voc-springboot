<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wumeng.voc.mapper.VocAnswerItemMapper">

    <select id="getComponentStat" resultType="com.wumeng.voc.vo.StatComponentItemVo">
        SELECT vai.value as name, count(1) as count
        FROM voc_answer_item vai
        WHERE survey_id = #{surveyId} AND component_id = #{componentId}
        GROUP BY vai.value
    </select>
</mapper>
